import { AddressType } from '@/features/users/types/user';

export const USER_ADDRESS_TYPE_LABEL: Record<AddressType, string> = {
    HOME: 'Home',
    INVOICE: 'Invoice',
    POST: 'Post',
    WORK: 'Work',
};
